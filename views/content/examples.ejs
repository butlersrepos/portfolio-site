<link rel="stylesheet" href="css/recreations.css"/>

<div id="recreationsPage">
	<div id="chatLog">
		{{ for(var i in records) { }}
			<div class="messageLine">
				<span class="userNameTag">{{=records[i].name}}</span> - <span class="messageTimestamp">{{=records[i].updated_time}}</span>: <span class="textLine">{{=records[i].words}}</span>
			</div>	
		{{ } }}
	</div>
</div>